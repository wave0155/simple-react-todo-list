import { React } from 'react';
import { View, Text } from 'react-native';

export default class TaskEditor extends React.Component {
  render() {
    return (
      <View><Text>Task Editor</Text></View>
    );
  }
}